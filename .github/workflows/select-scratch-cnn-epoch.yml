name: Select scratch CNN training epoch

on:
  push:
    branches: [raw-eda-scratch]
    paths:
      - "scripts/select_scratch_cnn_epoch.py"
      - "raw_model_selection/cnn_family_comparison_protocol.json"
      - ".github/workflows/select-scratch-cnn-epoch.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  select:
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4
        with:
          ref: raw-eda-scratch

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install gdown pillow pandas scikit-learn
          pip install torch==2.8.0 torchvision==0.23.0 --index-url https://download.pytorch.org/whl/cpu

      - name: Syntax check
        run: python -m py_compile scripts/select_scratch_cnn_epoch.py

      - name: Download authoritative dataset
        run: |
          mkdir -p data
          gdown 1KDQBTbo5deKGCdVV_xIujscn5ImxW4dm -O data/images.zip
          test "$(stat -c%s data/images.zip)" = "939921132"
          echo "033dd76fa617ba9bcf16d8ca4dcc294a838a6956eae0740f3013e4663805008f  data/images.zip" | sha256sum -c -
          rm -rf data/extracted
          mkdir -p data/extracted
          unzip -q data/images.zip -d data/extracted

      - name: Select scratch CNN epoch on D2 development only
        run: |
          rm -rf raw_scratch_epoch_selection
          python scripts/select_scratch_cnn_epoch.py \
            --dataset-root data/extracted \
            --manifest raw_model_selection/d2_model_selection_frame_manifest.csv \
            --comparison-protocol raw_model_selection/cnn_family_comparison_protocol.json \
            --archive data/images.zip \
            --out-dir raw_scratch_epoch_selection
          cat raw_scratch_epoch_selection/scratch_epoch_selection.json
          cat raw_scratch_epoch_selection/scratch_epoch_selection_receipt.json

      - name: Commit scratch epoch-selection receipts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f raw_scratch_epoch_selection/
          if git diff --cached --quiet; then
            echo "No scratch epoch-selection changes."
          else
            git commit -m "Lock scratch CNN training epoch"
            git pull --rebase origin raw-eda-scratch
            git push origin HEAD:raw-eda-scratch
          fi
